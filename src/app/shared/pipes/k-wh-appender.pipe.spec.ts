import { KWhAppenderPipe } from './k-wh-appender.pipe';

describe('KWhAppenderPipe', () => {
  it('create an instance', () => {
    const pipe = new KWhAppenderPipe();
    expect(pipe).toBeTruthy();
  });
});
